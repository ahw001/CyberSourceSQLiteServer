# Copilot Instructions

## Project Guidelines
- For PayPal create order, use the `/pts/v2/intents` endpoint (not `/pts/v2/payments`).
- If config changes aren’t reflected at runtime, do a clean + rebuild to ensure the updated appsettings.json is copied to output.